"homepage": "https://github.com/yenayw/Valentines.git",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}
git add .
git commit -m "Deploy React app"
git push origin main
npm run deploy